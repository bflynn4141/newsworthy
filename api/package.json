{
  "name": "newsworthy-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute newsworthy-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "viem": "^2.46.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250303.0",
    "wrangler": "^4.0.0"
  }
}
